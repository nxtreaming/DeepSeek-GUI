{
  "name": "@kun-examples/direct-dom",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../copy-static-assets.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "validate": "node ../run-repository-kun-cli.mjs extension validate .",
    "pack": "npm run build && node ../run-repository-kun-cli.mjs extension pack ."
  },
  "dependencies": {
    "@kun/extension-api": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.2"
  }
}
