# Starlight background artwork

`starlight-stage.svg` is the original, self-contained vector source for
`../img/starlight-stage.webp`. It uses no external images, fonts, or network resources.

The WebP export is 1600 × 900. Only the WebP is referenced by `manifest.json`; this
authoring directory is intentionally ignored when the plugin is installed.
